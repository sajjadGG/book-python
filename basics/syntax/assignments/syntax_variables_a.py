"""
* Assignment: Syntax Variables HelloWorld
* Complexity: easy
* Lines of code: 1 lines
* Time: 2 min

English:
    1. Define:
       a. variable `x` with value 1
       b. variable `y` with value 2
    2. Run doctests - all must succeed

Polish:
    1. Zdefiniuj:
       a. zmienną `x` z wartością 1
       b. zmienną `y` z wartością 2
    2. Uruchom doctesty - wszystkie muszą się powieść

Tests:
    >>> import sys; sys.tracebacklimit = 0

    >>> assert type(x) is int, \
    'Variable `x` has invalid type, should be int'
    >>> assert type(y) is int, \
    'Variable `y` has invalid type, should be int'
    >>> assert x == 1, \
    'Variable `x` has invalid value, should be 1'
    >>> assert y == 2, \
    'Variable `y` has invalid value, should be 2'
"""

# int: with value 1
x = ...

# int: with value 2
y = ...


# Solution
x = 1
y = 2
