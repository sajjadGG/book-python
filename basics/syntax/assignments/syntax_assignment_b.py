"""
* Assignment: Syntax Variables Float
* Complexity: easy
* Lines of code: 1 lines
* Time: 2 min

English:
    1. Define variable `result` with value 1.1
    2. Run doctests - all must succeed

Polish:
    1. Zdefiniuj zmienną `result` z wartością 1.1
    2. Uruchom doctesty - wszystkie muszą się powieść

Tests:
    >>> import sys; sys.tracebacklimit = 0

    >>> assert type(result) is float, \
    'Variable `result` has invalid type, should be float'
    >>> assert result == 1.1, \
    'Variable `result` has invalid value, should be 1.1'
"""

# int: with value 1.1
result = ...


# Solution
result = 1.1
